import React from 'react'
import { Line } from 'react-chartjs-2';
import { Chart as ChartJS, CatergoryScale, LinearScale, PointElement, LineElement, Title, Tooltip, Legend } from 'chart.js';

const LineChart = () => {
    return (
        <div>LineChart</div>
    )
}

export default LineChart